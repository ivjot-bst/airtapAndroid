/**
 * App Management Module for AirTap Android SDK
 * Exports the app manager for installing, launching, and managing apps
 */

const AppManager = require('./manager');

module.exports = {
  AppManager
};
